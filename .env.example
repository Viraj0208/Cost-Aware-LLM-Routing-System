# Application mode: simulation | local | production
APP_MODE=simulation

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Triton Server (production mode only)
TRITON_URL=localhost:8001
TRITON_PROTOCOL=grpc

# Routing
ROUTING_THRESHOLD=0.5
ROUTING_MAX_LATENCY_MS=10

# Logging
LOG_LEVEL=INFO
